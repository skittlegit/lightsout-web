# Must include the /api suffix — lib/api.ts builds paths as `${BASE}/calendar`,
# `${BASE}/predictions/next`, etc. Without /api every call 404s and the UI
# silently falls back to mock data (mock shows Miami GP + no predictions).
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_SEASON=2026
